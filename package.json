{
    "name": "mi-app-presupuestos",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
        "test": "echo \"No tests\"",
        "lint": "eslint ."
    },
    "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
